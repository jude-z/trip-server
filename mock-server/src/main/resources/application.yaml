server:
  port: ${SERVER_PORT:8082}

spring:
  datasource:
    url: jdbc:mysql://gathering.cvuk42kqur71.ap-northeast-2.rds.amazonaws.com:3306/mock
    username: root
    password: rootroot
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100
    open-in-view: false

webhook:
  url: http://52.78.132.75:8080/payment/webhook
